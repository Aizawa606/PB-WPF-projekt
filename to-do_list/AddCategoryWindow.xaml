<Window x:Class="to_do_list.AddCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:to_do_list"
        mc:Ignorable="d"
        Title="{DynamicResource AddCategoryWindow}" Height="150" Width="300">
    <StackPanel Margin="10">
        <TextBlock Text="{DynamicResource txt__nazwa_kategorii}" Margin="0,0,0,5"/>
        <TextBox x:Name="CategoryTextBox" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="60" Margin="0,0,10,0" Click="Ok_Click"/>
            <Button Content="{DynamicResource txt_anuluj}" Width="60" Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
